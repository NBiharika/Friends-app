<% provide(:friend_first_name, 'All Users') %>

<% if user_signed_in? %>

<h1>All Users</h1>
  <table class="table table-striped table-bordered table-hover">
    <thead class="thead-dark">
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Phone</th>
        <th>Twitter</th>
        <th> </th>
      </tr>
    </thead>

    <tbody>
        <% @users.each do |user| %> 
          <%# if user.id == current_user.friends.user_id %>
          <tr>
            <td>
              <%= user.first_name%>
              <%= user.last_name %>
            </td>

            <td><%= user.email %></td>
            <td><%= user.phone %></td>
            <td><%= user.twitter %></td>
          
            <td><%= link_to 'Add Friend', add_path(user), class:"btn btn-primary" %></td>
          </tr>
          <%# end %>
        <% end %>

      <%#= will_paginate @users %> 
     
      <%#= will_paginate @users %> 
    </tbody>
  </table>

  <br>
<% else %>

<h1>Hello World!</h1>
This is my first Rails Webpage...

<% end %>

